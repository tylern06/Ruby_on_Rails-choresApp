
<!-- Navigation -->
<nav class="navbar navbar-default navbar-fixed-top topnav" role="navigation">
    <div class="container topnav">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>

            <form action="/" action="get">
                <input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">
                <input type='image' src="/assets/swept1.png" class="img-responsive" width='180' height='90' alt='Home'>
            </form>
            
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                <li>
                    <a class="navbar-brand topnav" href="/chores">Chores</a>
                </li>
                <li>
                    <% if session[:login_type] == "option1" %>
                        <a class="navbar-brand topnav" href="/clients">Dashboard</a>
                        <% else %>
                        <a class="navbar-brand topnav" href="/contractors">Dashboard</a>
                    <% end %>
                </li>
                <li>
                    <a class="navbar-brand topnav" href="/clients/<%= session[:user_id] %>">Profile</a>
                </li>
                <li>
                    <% if !current_user %>
                    <li class="dropdown" id="menuLogin">
                        <a class="dropdown-toggle" href="#" data-toggle="dropdown" id="navLogin">Login</a>
                        <div class="dropdown-menu" style="padding:17px;">
                          <form action='/sessions' method='post' class="form" id="formLogin">
                            <input name='authenticity_token' value='<%= form_authenticity_token %>' type='hidden'> 
                            <label for="Email">Email: </label>
                            <input type="email" placeholder="Email" name='client[email]'>
                            <p><label for="Password">Password: </label>
                            <input type="password" placeholder="Password" name='client[password]'></p>
                            <div class="radio">
                                Login as:
                              <label>
                                <input type="radio" name="optionsRadios" id="optionsRadios1" value="option1" checked>
                                Client
                              </label>
                            </div>
                            <div class="radio">
                              <label>
                                <input type="radio" name="optionsRadios" id="optionsRadios2" value="option2">
                                Contractor
                              </label>
                            </div>
                            <input type="submit" value='Login'>
                          </form>
                        </div>
                    </li>
                <% else %>
                    <a class="navbar-brand topnav" href='/sessions/<%= session[:user_id] %>'data-method='delete'>Logout</a>
                <% end %>    
                </li>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container -->
</nav>